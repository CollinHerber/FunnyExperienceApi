using System;
using System.Linq;
using System.Security.Claims;

namespace FunnyExperience.Server.Lib.Extensions;

public static class ClaimsPrincipalExtensions
{
    //public static long GetId(this ClaimsPrincipal user) =>
    //    Convert.ToInt64(user.Claims.FirstOrDefault(y => y.Type == ClaimTypes.NameIdentifier));
}